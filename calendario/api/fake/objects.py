class Calendario(object):
	def __init__(self, id, start, end, url, title, setor, pessoa, cclass):
		self.id = id
		self.start = start
		self.end = end
		self.url = url
		self.title = title
		self.setor = setor
		self.pessoa = pessoa
		self.cclass = cclass